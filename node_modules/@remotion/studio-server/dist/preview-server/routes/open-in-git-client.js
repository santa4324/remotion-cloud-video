"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.openInGitClientHandler = void 0;
const git_client_registry_1 = require("../../helpers/git-client-registry");
const openInGitClientHandler = async ({ input, remotionRoot }) => {
    const gitClient = (await (0, git_client_registry_1.getAvailableGitClients)()).find(({ id }) => id === input.gitClientId);
    if (!gitClient) {
        return { success: false };
    }
    await (0, git_client_registry_1.launchGitClient)({ gitClient, remotionRoot });
    return { success: true };
};
exports.openInGitClientHandler = openInGitClientHandler;
