import { type SequenceNodePathRemapping } from '@remotion/studio-shared';
import type { SequenceNodePath } from 'remotion';
export declare const splitJsxSequence: ({ input, nodePath, sequenceKeys, splitFrame, formatFile, prettierConfigOverride, }: {
    input: string;
    nodePath: SequenceNodePath;
    sequenceKeys: string[];
    splitFrame: number;
    formatFile: (input: {
        contents: string;
        prettierConfigOverride: Record<string, unknown> | null;
    }) => Promise<{
        output: string;
        formatted: boolean;
    }>;
    prettierConfigOverride?: Record<string, unknown> | null | undefined;
}) => Promise<{
    output: string;
    formatted: boolean;
    nodeLabel: string;
    logLine: number;
    nodePathRemappings: SequenceNodePathRemapping[];
}>;
