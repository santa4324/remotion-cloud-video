import type { ReorderSequencePosition, SequenceNodePathRemapping } from '@remotion/studio-shared';
import type { SequenceNodePath } from 'remotion';
export declare const reorderSequence: ({ input, sourceNodePath, targetNodePath, position, formatFile, prettierConfigOverride, }: {
    input: string;
    sourceNodePath: SequenceNodePath;
    targetNodePath: SequenceNodePath;
    position: ReorderSequencePosition;
    formatFile: (input: {
        contents: string;
        prettierConfigOverride: Record<string, unknown> | null;
    }) => Promise<{
        output: string;
        formatted: boolean;
    }>;
    prettierConfigOverride?: Record<string, unknown> | null | undefined;
}) => Promise<{
    output: string;
    formatted: boolean;
    sequenceLabel: string;
    logLine: number;
    nodePathRemappings: SequenceNodePathRemapping[];
}>;
