import type { VideoSampleSink } from 'mediabunny';
import { type KeyframeBank } from './keyframe-bank';
export declare const makeKeyframeManager: ({ getTotalCacheStats, }: {
    getTotalCacheStats: () => {
        count: number;
        totalSize: number;
    };
}) => {
    requestKeyframeBank: ({ timestamp, videoSampleSink, src, logLevel, maxCacheSize, fps, }: {
        timestamp: number;
        videoSampleSink: VideoSampleSink;
        src: string;
        logLevel: "error" | "info" | "trace" | "verbose" | "warn";
        maxCacheSize: number;
        fps: number;
    }) => Promise<KeyframeBank | null>;
    getCacheStats: () => {
        count: number;
        totalSize: number;
    };
    clearAll: (logLevel: "error" | "info" | "trace" | "verbose" | "warn") => void;
    dispose: (logLevel: "error" | "info" | "trace" | "verbose" | "warn") => void;
};
export type KeyframeManager = Awaited<ReturnType<typeof makeKeyframeManager>>;
