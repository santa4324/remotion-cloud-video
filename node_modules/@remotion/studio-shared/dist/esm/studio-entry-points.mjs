// src/studio-entry-points.ts
var getStudioEntryPoints = ({
  fastRefreshRuntime,
  reactScan,
  environmentSetup,
  sequenceStackTraces,
  userDefinedComponent,
  reactShim,
  studioRenderEntry
}) => [
  fastRefreshRuntime,
  reactScan,
  environmentSetup,
  sequenceStackTraces,
  userDefinedComponent,
  reactShim,
  studioRenderEntry
].filter(Boolean);
export {
  getStudioEntryPoints
};
